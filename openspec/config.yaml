schema: spec-driven

context: |
  Community spec for native Energy Management in openHAB (seed for openhab-core#3478).
  Goal: an EMS framework in openhab-core plus new add-on extension points — NOT a binding.
  Language: Java 21, openhab-core conventions (OSGi, registries/providers, item metadata, UoM).
  Already available in core and to be built upon, not reinvented:
    - future-timestamped TimeSeries + the `forecast` persistence strategy (openHAB 4.1)
    - Number:Currency / Number:EnergyPrice UoM (openHAB 4.1)
    - item metadata namespaces, the semantic model, persistence services
  Requirements are mechanism-neutral SHALL statements; contested design picks live in
  design.md files as open questions, not in requirements.
  Every requirement carries a Source line pointing at the #3478 comment or production
  system it came from (storm.house, openhab-spot-price-optimizer, openhab-binding-emsmanager).
  Wave order: 1 spine (participant model, engine contract, extension points, energy
  levels, + semantic-model discovery as onboarding), 2 data planes (prices, forecasts,
  optimization objectives), 3 grid constraints + named profiles + energy UI (setup and
  visualization), 4 adaptive learning. Executable acceptance vectors live in fixtures/.

  Implementation must follow the openHAB coding guidelines
  (https://www.openhab.org/docs/developer/guidelines.html — also the PR review checklist):
    - Java 21; OSGi R8 with Declarative Services annotations (@Component/@Activate/@Reference).
    - SLF4J logging only; logger is non-static final, named "logger".
    - EPL-2.0 license header on every Java file (mvn license:format adds missing ones).
    - @NonNullByDefault on every class except DTOs; code must compile without warnings.
    - Classes live under .internal packages unless they are cross-bundle/script API.
    - JavaDoc + @author tags on every class/interface and default+ visible member (DTOs exempt).
    - Formatting enforced by Spotless (mvn spotless:apply); XML 120 chars, tabs (pom: 2 spaces).
    - Naming: thing/channel-type ids lower-case-hyphen, config parameters camelCase.
    - Never create threads — use the shared schedulers; prefer scheduleWithFixedDelay.
    - Bundles must start/stop cleanly and stay off the CPU when idle.
    - Logging levels: error = setup tremendously wrong / irrecoverable failure worth a bug
      report; warn = something seems wrong but the system still functions, or recoverable
      abnormal paths; info = sparse (component started, file loaded); debug/trace = detail.
      Parameterized logging only (no string concatenation); exceptions with stacktraces in
      the log are treated as bugs; logging never replaces the framework's update* methods.
    - Null annotations detail: @Nullable on nullable fields/returns;
      @NonNullByDefault({}) for DS-injected references; DTOs live in a dto package or
      carry a DTO suffix.
    - Static code analysis (openhab/static-code-analysis) runs in the build; any
      priority-1 finding fails the build.
    - External dependencies only from the approved default-library list (gson, jetty
      client/websocket, xstream, jax-rs client/core/sse, ...); anything else needs
      maintainer discussion first.
    - Copied code must be EPL-2.0-compatible (Apache, EPL-1.0, MIT, BSD); keep author
      attributions and license headers, and record filename/author/license in the
      bundle's NOTICE file (short snippets exempt).
    - Markdown follows the openhab-docs markdownlint config (this repo lints clean
      against it).
  REVIEW RULES ARE GOVERNING — see docs/REVIEW_CHECKLIST.md (reproduces the openHAB
  maintainer rules, the 44-item Review Checklist and the org-wide merge rules).
  Reviewers weight, in this order: (1) consistency towards the user, (2) architecture
  consistency - use the openHAB APIs and provided features rather than re-inventing
  them, (3) consistent runtime behaviour - status handling, clean shutdown, proper
  logging, and explicitly NOT individual code style. Design for the first three.
  Checklist items that apply to core code as well as add-ons:
    - camelCase non-static fields (no underscores or prefixes); prefer primitives to
      wrappers; refactor duplicate code
    - every byte[]/String conversion and stream specifies a Charset; sockets and streams
      use try-with-resources; lambdas for Runnables
    - custom exceptions extend Exception (checked); never throw RuntimeException for
      expected errors; never catch generic Exception (catch RuntimeException for
      unexpected ones); special catch for InterruptedIOException beside IOException and
      return immediately on InterruptedException/InterruptedIOException
    - cache getConfigAs()/getConfiguration() results; annotate unavoidable warnings with
      @SuppressWarnings; no need to test a Future for cancellation before cancelling
    - any unavoidable thread is daemon and named; scrutinise synchronized methods for
      deadlock
    - conservative log levels (mostly debug; error for bugs/misconfiguration); stack
      traces only for severe errors that indicate a bug
    - before submitting: review target/code-analysis/report.html, run
      mvn spotless:check -Dspotless.check.skip=false, and validate with mvn javadoc:javadoc
  Contribution process (openhab-core CONTRIBUTING.md):
    - Discuss ambitious designs on the community forum before coding.
    - Feature branches tie to an enhancement issue, named XXX-something.
    - Unit tests are required; the full suite runs green before a PR and after every commit.
    - Documentation changes land in the same commit as the feature.
    - Commit subject: imperative, capitalized, max 50 chars; squash to logical units.
      Reference issues with "Closes #NNN" / "Fixes #NNN" to auto-close them.
    - Every commit carries a DCO sign-off (Signed-off-by: Real Name <email>). The
      small-patch exception (typos, single-line doc/comment changes) is the only waiver.
    - MULTI-AUTHOR COMMITS USE openHAB's OWN TRAILER: "Also-by: Name <email>" placed
      BEFORE the Signed-off-by line — not GitHub's Co-Authored-By — for anything destined
      for an openHAB repository (contributing.html, DCO section).
    - Before coding: discuss substantial work on the community forum, check for an
      existing issue, and open an enhancement issue for significant improvements; branch
      names are NNN-something after that issue. Documentation-only changes need no issue.
    - Target repositories and their split: openhab-core (runtime framework),
      openhab-addons (bindings and services), openhab-webui (MainUI and friends),
      openhab-distro (assembly). This corpus targets core, with UI in webui and any
      device/protocol integration in addons.
  DEFAULT LIBRARIES — using anything outside this set needs maintainer discussion first
  (guidelines.html#default-libraries):
    - XML: com.thoughtworks.xstream(.annotations/.converters/.io/.io.xml)
    - JSON: com.google.gson.*
    - HTTP: org.eclipse.jetty.client.*, .client.api.*, .http.*, .util.* — and obtain
      HttpClient instances from the HttpClientFactory service, using the SHARED instance
      unless there are specific configuration requirements
    - WebSocket: org.eclipse.jetty.websocket.client/.api — obtain WebSocketClient from
      the WebSocketClientFactory service, shared instance by default
    - SSE: javax.ws.rs.client/.core/.sse
    Using the provided shared clients rather than creating your own is exactly the
    "architecture consistency" reviewers weight second-highest.

rules:
  proposal:
    - Always include a "Non-goals" section
    - Name the wave the change belongs to
  specs:
    - Every requirement ends with a "Source:" line citing its origin
    - One SHALL per requirement; scenarios use GIVEN/WHEN/THEN
  tasks:
    - Implementation-phase tasks end with spotless:apply, a green full test suite, and DCO-signed commits
